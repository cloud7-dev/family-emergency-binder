{
  "name": "family-emergency-binder",
  "version": "1.1.0",
  "private": false,
  "description": "ReadyBinder is a local-first encrypted emergency vault for families.",
  "type": "commonjs",
  "scripts": {
    "serve": "python3 -m http.server 8765",
    "test": "node --check app.js && node --check scripts/static-check.js && node --check scripts/qa-headless.js && node --check scripts/security-test.js && node scripts/static-check.js",
    "security:test": "node scripts/security-test.js",
    "qa:browser": "node scripts/qa-headless.js"
  },
  "keywords": [
    "readybinder",
    "emergency-binder",
    "emergency-vault",
    "family-emergency-binder",
    "local-first",
    "privacy-first",
    "encrypted-vault",
    "encrypted-attachments",
    "attachment-preview",
    "offline-vault",
    "structured-vault",
    "emergency-preparedness",
    "pwa"
  ],
  "license": "Apache-2.0"
}
