{
  "name": "family-emergency-binder",
  "version": "0.3.0",
  "private": false,
  "description": "A local-first encrypted emergency binder for families.",
  "type": "commonjs",
  "scripts": {
    "serve": "python3 -m http.server 8765",
    "test": "node --check app.js && node --check scripts/static-check.js && node --check scripts/qa-headless.js && node scripts/static-check.js",
    "qa:browser": "node scripts/qa-headless.js"
  },
  "keywords": [
    "emergency-binder",
    "family-emergency-binder",
    "local-first",
    "privacy-first",
    "encrypted-vault",
    "encrypted-attachments",
    "pwa"
  ],
  "license": "Apache-2.0"
}
